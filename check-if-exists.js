// Check if class doesn't exist
var $hero = $('.hero');
var $cta = $('.cta-connect');
if(($hero).length === 0 ) {
  $cta.hide();
}

// Check if class exists
var $table = $('.table-wrapper');
if(($table).length > 0 ) {

}